# A collection of security papers on top publications.

Up to now, four top conferences are supported.

They are:

- IEEE S&P (oakland)
- USENIX Security Symposium (USENIX Sec)
- ACM CCS
- NDSS

PRs and issues are warmly welcomed.

To update, simply update `config.yml` and run `main.py` to crawl the latest information.

