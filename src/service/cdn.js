// Base URL for CDN-hosted data files.
// Backed by Aliyun OSS bucket `premium-cdn` (object prefix `sec-papers/`),
// served through the CDN domain cdn.c01dkit.com.
export const CDN_DATA_BASE = 'https://cdn.c01dkit.com/sec-papers';
